<%= form_for(@system_type) do |f| %>
  <% if @system_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@system_type.errors.count, "error") %> prohibited this system_type from being saved:</h2>

      <ul>
      <% @system_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :icon %><br />
    <%= f.text_field :icon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
