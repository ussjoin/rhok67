<%= form_for(@hardware_type) do |f| %>
  <% if @hardware_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hardware_type.errors.count, "error") %> prohibited this hardware_type from being saved:</h2>

      <ul>
      <% @hardware_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :icon %><br />
    <%= f.text_field :icon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
